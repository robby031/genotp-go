run:
  timeout: 5m
  tests: true
  modules-download-mode: readonly

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gosec
    - gofmt
    - goimports
    - misspell
    - revive
    - unconvert
    - unparam
    - gocritic

linters-settings:
  errcheck:
    check-type-assertions: false
    check-blank: true
  
  govet:
    enable-all: true
    disable:
      - fieldalignment
      - shadow
  
  gosec:
    excludes:
      - G104  # We handle errors appropriately
      - G401  # We use MD5/SHA1 intentionally for HOTP/TOTP
      - G505  # SHA1 is required by HOTP/TOTP RFC 4226/6238
      - G115  # Integer overflow conversions are intentional and safe here
  
  revive:
    rules:
      - name: exported
        disabled: true
      - name: var-naming
        disabled: true
      - name: package-comments
        disabled: true

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0
  
  exclude-rules:
    # Exclude certain linters from test files
    - path: _test\.go
      linters:
        - errcheck
        - gosec
        - unparam
        - ineffassign
        - shadow
    
    # Exclude fuzz test files from certain checks
    - path: fuzz/.*_fuzz_test\.go
      linters:
        - errcheck
        - gosec

output:
  formats:
    - format: colored-line-number
  print-issued-lines: true
  print-linter-name: true